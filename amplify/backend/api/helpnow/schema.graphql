type UserCaseDetails @model @auth(rules: [{ allow: public }]) {
  caseId: ID! @primaryKey
  name: String!
  description: String!
  location: String
  createdAt: AWSDateTime
  caseType: String
  status: String
}
